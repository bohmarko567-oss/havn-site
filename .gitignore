# temp / generator files
_*.html
_*.png
.DS_Store
Thumbs.db

# deploy / tooling
.vercel
.netlify
node_modules
package-lock.json

# secrets — NEVER commit keys
.env
.env.*
!.env.example

.claude/

# owner runbook — never in the public repo
GO_LIVE.md

# env template lives with the runbook, not the public repo
.env.example
